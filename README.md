# Flask CI App

Simple Flask app for CI demonstration. Contains a home route and a pytest test.

Quick start:

```bash
python -m venv .venv
.venv\Scripts\activate  # On Windows
pip install -r requirements.txt
pytest
```
